<h1><%= @artist.name %></h1>

<p><%= @artist.bio %></p>

<h2>Artists songs:</h2>

<ul>
    <%= @artist.songs.each do |song| %>
        <li>
            <%= link_to song.name, song_path(song) %>
        </li>
    <% end %>
</ul>